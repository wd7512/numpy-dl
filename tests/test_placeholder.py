"""Placeholder test so CI has something to run before v0.1.0 lands.

Replace/remove this once real layer, loss, and optimizer tests exist
under tests/nn/, tests/rl/, etc.
"""

import numpy_dl


def test_package_importable():
    assert numpy_dl.__version__ == "0.0.1"
